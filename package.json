{
  "name": "inbox-to-action-extractor-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "npm run build && node --test dist/tests/**/*.test.js",
    "typecheck": "tsc --noEmit",
    "generate:secret": "node scripts/generate_api_key.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.20.0"
  }
}
